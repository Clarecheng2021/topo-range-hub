# This basic template contains no cloud credentials or production network settings.
# Keep the host binding in docker-compose.yml on 127.0.0.1 unless a supervised lab
# network is explicitly required.
